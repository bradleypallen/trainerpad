{
  "name": "trainerpad",
  "version": "1.0.0",
  "description": "Local-first client-management PWA for personal trainers — builds to a single HTML file, data lives in IndexedDB on the device.",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.56.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
